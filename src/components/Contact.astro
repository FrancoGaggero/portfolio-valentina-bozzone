---
---

<section id="contact" class="py-20 px-6 bg-white">
  <div class="container mx-auto max-w-6xl">
    
    <!-- Elegant Header -->
    <div class="text-center mb-20 fade-in">
      <h2 class="text-5xl md:text-6xl font-display font-light mb-6 text-black tracking-tight">
        Trabajemos Juntos
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto leading-relaxed">
        Cada proyecto es una oportunidad para crear algo extraordinario. 
        Cuéntame tu visión y hagámosla realidad.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-20 items-start">
      
      <!-- Contact Information -->
      <div class="fade-in">
        <div class="space-y-12">
          
          <!-- Contact Methods -->
          <div>
            <h3 class="text-2xl font-display font-light text-black mb-8">Conectemos</h3>
            <div class="space-y-6">
              
              <div class="group">
                <div class="flex items-center space-x-4 py-4 border-b border-gray-100 group-hover:border-black transition-colors duration-300">
                  <div class="w-12 h-12 bg-black rounded-full flex items-center justify-center">
                    <span class="text-white text-lg">✉</span>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500 uppercase tracking-wide">Email</p>
                    <a href="mailto:valentina@portfolio.com" class="text-lg text-black hover:text-gray-600 transition-colors">
                      valentina@portfolio.com
                    </a>
                  </div>
                </div>
              </div>

              <div class="group">
                <div class="flex items-center space-x-4 py-4 border-b border-gray-100 group-hover:border-black transition-colors duration-300">
                  <div class="w-12 h-12 bg-black rounded-full flex items-center justify-center">
                    <span class="text-white text-lg">📱</span>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500 uppercase tracking-wide">WhatsApp</p>
                    <a href="https://wa.me/5491127567099" class="text-lg text-black hover:text-gray-600 transition-colors" target="_blank">
                        +54 9 11 2756-7099
                    </a>
                  </div>
                </div>
              </div>

              <div class="group">
                <div class="flex items-center space-x-4 py-4 border-b border-gray-100 group-hover:border-black transition-colors duration-300">
                  <div class="w-12 h-12 bg-black rounded-full flex items-center justify-center">
                    <span class="text-white text-lg">📍</span>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500 uppercase tracking-wide">Ubicación</p>
                    <p class="text-lg text-black">Buenos Aires, Argentina</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Availability -->
          <div>
            <h3 class="text-2xl font-display font-light text-black mb-8">Disponibilidad</h3>
            <div class="bg-gray-50 p-8 rounded-lg">
              <div class="flex items-center space-x-3 mb-4">
                <div class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></div>
                <p class="text-black font-medium">Disponible para nuevos proyectos</p>
              </div>
              <p class="text-gray-600 text-sm leading-relaxed">
                Tiempo de respuesta típico: 24-48 horas.<br>
                Consultas urgentes: WhatsApp directo.
              </p>
            </div>
          </div>
        </div>
      </div>


      <!--activar el formularios registrandose en formspree.io con mail de valen -->
      <!-- Contact Form -->
      <div class="fade-in">
        <form 
          id="contactForm" 
          action="https://formspree.io/f/YOUR_FORM_ID" 
          method="POST"
          class="space-y-6"
        >
          
          <!-- Name and Email Row -->
          <div class="grid md:grid-cols-2 gap-6">
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 mb-2">Nombre *</label>
              <input 
                type="text" 
                id="name" 
                name="name" 
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black placeholder-gray-400 focus:border-black focus:outline-none transition-colors duration-300"
                placeholder="Tu nombre completo"
              >
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email *</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                required
                class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black placeholder-gray-400 focus:border-black focus:outline-none transition-colors duration-300"
                placeholder="tu@email.com"
              >
            </div>
          </div>

          <!-- Company and Phone Row -->
          <div class="grid md:grid-cols-2 gap-6">
            <div>
              <label for="company" class="block text-sm font-medium text-gray-700 mb-2">Empresa</label>
              <input 
                type="text" 
                id="company" 
                name="company"
                class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black placeholder-gray-400 focus:border-black focus:outline-none transition-colors duration-300"
                placeholder="Nombre de tu empresa"
              >
            </div>
            <div>
              <label for="phone" class="block text-sm font-medium text-gray-700 mb-2">Teléfono</label>
              <input 
                type="tel" 
                id="phone" 
                name="phone"
                class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black placeholder-gray-400 focus:border-black focus:outline-none transition-colors duration-300"
                placeholder="+54 9 11 ..."
              >
            </div>
          </div>

          <!-- Project Type -->
          <div>
            <label for="projectType" class="block text-sm font-medium text-gray-700 mb-2">Tipo de Proyecto *</label>
            <select 
              id="projectType" 
              name="projectType" 
              required
              class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black focus:border-black focus:outline-none transition-colors duration-300"
            >
              <option value="">Selecciona un tipo de proyecto</option>
              <option value="produccion">Producción de Moda</option>
              <option value="fotografia">Fotografía</option>
              <option value="community">Community Management</option>
              <option value="estrategia">Estrategia Digital</option>
              <option value="branding">Branding</option>
              <option value="consultoria">Consultoría</option>
              <option value="otro">Otro</option>
            </select>
          </div>

          <!-- Budget Range -->
          <div>
            <label for="budget" class="block text-sm font-medium text-gray-700 mb-2">Presupuesto Estimado</label>
            <select 
              id="budget" 
              name="budget"
              class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black focus:border-black focus:outline-none transition-colors duration-300"
            >
              <option value="">Selecciona un rango</option>
              <option value="bajo">$1,000 - $5,000 USD</option>
              <option value="medio">$5,000 - $15,000 USD</option>
              <option value="alto">$15,000 - $50,000 USD</option>
              <option value="premium">$50,000+ USD</option>
              <option value="consultar">Prefiero consultar</option>
            </select>
          </div>

          <!-- Timeline -->
          <div>
            <label for="timeline" class="block text-sm font-medium text-gray-700 mb-2">Timeline Deseado</label>
            <select 
              id="timeline" 
              name="timeline"
              class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black focus:border-black focus:outline-none transition-colors duration-300"
            >
              <option value="">Selecciona timeline</option>
              <option value="urgente">Urgente (1-2 semanas)</option>
              <option value="rapido">Rápido (1 mes)</option>
              <option value="normal">Normal (2-3 meses)</option>
              <option value="flexible">Flexible (3+ meses)</option>
            </select>
          </div>

          <!-- Message -->
          <div>
            <label for="message" class="block text-sm font-medium text-gray-700 mb-2">Mensaje *</label>
            <textarea 
              id="message" 
              name="message" 
              rows="6" 
              required
              class="w-full px-0 py-3 bg-transparent border-0 border-b-2 border-gray-200 text-black placeholder-gray-400 focus:border-black focus:outline-none transition-colors duration-300 resize-none"
              placeholder="Cuéntame sobre tu proyecto, objetivos, visión y cualquier detalle relevante..."
            ></textarea>
          </div>

          <!-- Submit Button -->
          <div class="pt-6">
            <button 
              type="submit" 
              class="w-full py-4 bg-black hover:bg-gray-800 text-white font-medium transition-all duration-300 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-black focus:ring-offset-2"
            >
              Enviar Mensaje
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contactForm') as HTMLFormElement;
    
    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
      if (!submitBtn) return;
      
      const originalText = submitBtn.textContent || 'Enviar Mensaje';
      
      // Loading state
      submitBtn.textContent = 'Enviando...';
      submitBtn.disabled = true;
      submitBtn.classList.add('opacity-75');
      
      try {
        // Envío real del formulario
        const formData = new FormData(form);
        const response = await fetch(form.action, {
          method: 'POST',
          body: formData,
          headers: {
            'Accept': 'application/json'
          }
        });
        
        if (response.ok) {
          // Éxito
          submitBtn.textContent = '¡Mensaje Enviado!';
          submitBtn.classList.remove('bg-black', 'hover:bg-gray-800');
          submitBtn.classList.add('bg-green-600');
          
          // Mostrar mensaje de éxito
          showNotification('¡Gracias! Tu mensaje ha sido enviado correctamente. Te responderé pronto.', 'success');
          
          // Reset form after success
          setTimeout(() => {
            submitBtn.textContent = originalText;
            submitBtn.disabled = false;
            submitBtn.classList.remove('opacity-75', 'bg-green-600');
            submitBtn.classList.add('bg-black', 'hover:bg-gray-800');
            form.reset();
          }, 3000);
        } else {
          throw new Error('Error en el envío');
        }
      } catch (error) {
        // Error
        submitBtn.textContent = 'Error al enviar';
        submitBtn.classList.remove('bg-black', 'hover:bg-gray-800');
        submitBtn.classList.add('bg-red-600');
        
        showNotification('Hubo un error al enviar el mensaje. Por favor, intenta nuevamente o contáctame directamente por WhatsApp.', 'error');
        
        // Reset button after error
        setTimeout(() => {
          submitBtn.textContent = originalText;
          submitBtn.disabled = false;
          submitBtn.classList.remove('opacity-75', 'bg-red-600');
          submitBtn.classList.add('bg-black', 'hover:bg-gray-800');
        }, 3000);
      }
    });
    
    // Función para mostrar notificaciones
    function showNotification(message: string, type: 'success' | 'error') {
      const notification = document.createElement('div');
      notification.className = `fixed top-8 right-8 max-w-md p-6 rounded-lg shadow-lg z-50 transition-all duration-300 ${
        type === 'success' 
          ? 'bg-green-100 border border-green-400 text-green-800' 
          : 'bg-red-100 border border-red-400 text-red-800'
      }`;
      
      notification.innerHTML = `
        <div class="flex items-start space-x-3">
          <div class="flex-shrink-0">
            <span class="text-xl">${type === 'success' ? '✅' : '❌'}</span>
          </div>
          <div class="flex-1">
            <p class="text-sm font-medium">${message}</p>
          </div>
          <button class="flex-shrink-0 text-gray-400 hover:text-gray-600" onclick="this.parentElement.parentElement.remove()">
            <span class="text-lg">×</span>
          </button>
        </div>
      `;
      
      document.body.appendChild(notification);
      
      // Auto remove after 6 seconds
      setTimeout(() => {
        if (notification.parentElement) {
          notification.remove();
        }
      }, 6000);
    }
  });
</script>
