---
// Portfolio simple con pocos proyectos destacados
const portfolioItems = [
  { 
    id: 1,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/agus5.jpeg","images/agus4.jpeg","images/agus1.jpeg","images/agus3.jpeg", "images/agus2.jpeg","images/agus6.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },
  {
    id: 10,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/tama4.jpeg","images/tama2.jpeg","images/tama1.jpeg", "images/tama3.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },



  /// produccion 1
  {
    id: 2,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/primera1.jpeg", "images/p1.jpeg","images/p2.jpeg","images/p3.jpeg", "images/p4.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },

  { 
    id: 9,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/clara4.jpeg", "images/clara2.jpeg","images/clara3.jpeg","images/clara1.jpeg", "images/clara5.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },

  { 
    id: 11,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/tamara3.jpeg", "images/tamara1.jpeg","images/tamara2.jpeg","images/tamara4.jpeg" ], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },


    {
    id: 3,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/cuarto1.jpeg", "images/cuarto2.jpeg","images/cuarto3.jpeg","images/cuarto4.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },
  


    {
    id: 4,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/primera.jpeg", "images/primera2.jpeg","images/primera3.jpeg","images/primera4.jpeg","images/primera5.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },
  {
    id: 5,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/cuarto5.jpeg", "images/cuarto6.jpeg","images/cuarto7.jpeg","images/cuarto8.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },

      { 
    id: 6,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/agus9.jpeg","images/agus7.jpeg", "images/agus8.jpeg","images/agus10.jpeg", "images/agus11.jpeg", "images/agus12.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },

    { 
    id: 7,
    title: "Producción de Moda",
    category: "Producción Fotográfica",
    images: ["images/cuarto9.jpeg", "images/cuarto10.jpeg","images/cuarto11.jpeg","images/cuarto12.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y Producción.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
  },
   { 
    id: 8,
    title: "Producción de Moda",
    category: "Producción Fotográfica UNITIVO",
    images: ["images/unitivo1.jpeg", "images/unitivo2.jpeg","images/unitivo3.jpeg","images/unitivo4.jpeg", "images/unitivo5.jpeg", "images/unitivo6.jpeg"], // Carrusel para este proyecto
    description: "Estilismo y producción para Nico Goldschmidt.",
    photographer: "Flor Zoccola", // Solo nombre, el Instagram va en photographerLink
    photographerLink: "https://instagram.com/florzoccola.fotos" // Link directo
   
  }
 

];
---

<section id="portfolio" class="py-20 px-6 bg-white">
  <div class="container mx-auto max-w-6xl">
    
    <!-- Simple Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-display font-light mb-4 text-black">
        Portfolio
      </h2>
      <p class="text-gray-600 max-w-md mx-auto">
        Algunos de mis trabajos más recientes
      </p>
    </div>

    <!-- Simple Grid -->
    <div class="grid md:grid-cols-2 gap-8">
      {portfolioItems.map((item) => (
        <div class="group cursor-pointer">
          <!-- Image or Carousel -->
          <div class="aspect-square bg-gray-100 mb-4 overflow-hidden relative">
            {item.images ? (
              <!-- Carousel for first project -->
              <div class="carousel-container relative w-full h-full" data-carousel={`carousel-${item.id}`}>
                {item.images.map((img, index) => (
                  <div 
                    class={`carousel-slide absolute inset-0 transition-opacity duration-500 ${index === 0 ? 'opacity-100' : 'opacity-0'}`}
                    data-slide={index}
                  >
                    <img 
                      src={img} 
                      alt={`${item.title} - Imagen ${index + 1}`}
                      class="w-full h-full object-contain transition-transform duration-500 group-hover:scale-105"
                    />
                  </div>
                ))}
                
                <!-- Navigation arrows -->
                <button 
                  class="carousel-prev absolute left-2 top-1/2 transform -translate-y-1/2 w-8 h-8 bg-black/50 hover:bg-black/70 text-white rounded-full flex items-center justify-center transition-all duration-300 opacity-0 group-hover:opacity-100"
                  data-carousel={`carousel-${item.id}`}
                  data-direction="prev"
                >
                  <span class="text-sm">‹</span>
                </button>
                
                <button 
                  class="carousel-next absolute right-2 top-1/2 transform -translate-y-1/2 w-8 h-8 bg-black/50 hover:bg-black/70 text-white rounded-full flex items-center justify-center transition-all duration-300 opacity-0 group-hover:opacity-100"
                  data-carousel={`carousel-${item.id}`}
                  data-direction="next"
                >
                  <span class="text-sm">›</span>
                </button>

                <!-- Navigation dots -->
                <div class="absolute bottom-4 left-1/2 transform -translate-x-1/2 flex space-x-2">
                  {item.images.map((_, index) => (
                    <button 
                      class={`carousel-dot w-2 h-2 rounded-full transition-all duration-300 ${index === 0 ? 'bg-white' : 'bg-white/50'}`}
                      data-slide-to={index}
                      data-carousel={`carousel-${item.id}`}
                    ></button>
                  ))}
                </div>
              </div>
            ) : (
              <!-- Single placeholder for other projects -->
              <div class="w-full h-full bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center transition-transform duration-500 group-hover:scale-105">
                <div class="text-4xl opacity-30">📸</div>
              </div>
            )}
          </div>
          
          <!-- Info -->
          <div>
            <p class="text-xs text-gray-500 uppercase tracking-wide mb-1">{item.category}</p>
            <h3 class="text-lg font-medium text-black mb-2">{item.title}</h3>
            <p class="text-gray-600 text-sm mb-2">{item.description}</p>
            
            <!-- Photographer credit (only if provided) -->
            {item.photographer && (
              <div class="flex items-center space-x-2 mt-3 pt-2 border-t border-gray-100">
                <span class="text-xs text-gray-500">Fotografía:</span>
                {item.photographerLink ? (
                  <a 
                    href={item.photographerLink}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-xs font-medium text-black hover:text-gray-600 transition-colors duration-300"
                  >
                    {item.photographer}
                  </a>
                ) : (
                  <span class="text-xs font-medium text-gray-700">{item.photographer}</span>
                )}
              </div>
            )}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  // Navegación rápida para carruseles sin animaciones lentas
  document.addEventListener('DOMContentLoaded', () => {
    const carousels = document.querySelectorAll('[data-carousel]');
    
    carousels.forEach(carousel => {
      const slides = carousel.querySelectorAll('.carousel-slide');
      const dots = carousel.querySelectorAll('.carousel-dot');
      const prevBtn = carousel.querySelector('.carousel-prev');
      const nextBtn = carousel.querySelector('.carousel-next');
      let currentSlide = 0;
      
      // Función para mostrar slide específico - sin animaciones lentas
      function showSlide(slideIndex: number) {
        slides.forEach((slide, index) => {
          const htmlSlide = slide as HTMLElement;
          if (index === slideIndex) {
            htmlSlide.style.opacity = '1';
          } else {
            htmlSlide.style.opacity = '0';
          }
        });
        
        // Actualizar dots sin animaciones
        dots.forEach((dot, index) => {
          if (index === slideIndex) {
            dot.classList.remove('bg-white/50');
            dot.classList.add('bg-white');
          } else {
            dot.classList.remove('bg-white');
            dot.classList.add('bg-white/50');
          }
        });
      }
      
      // Navegación con flechas
      if (prevBtn) {
        prevBtn.addEventListener('click', () => {
          currentSlide = currentSlide === 0 ? slides.length - 1 : currentSlide - 1;
          showSlide(currentSlide);
        });
      }
      
      if (nextBtn) {
        nextBtn.addEventListener('click', () => {
          currentSlide = (currentSlide + 1) % slides.length;
          showSlide(currentSlide);
        });
      }
      
      // Navegación con dots
      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          currentSlide = index;
          showSlide(currentSlide);
        });
      });
    });
  });
</script>
